# Source_to_html
A C-based tool that converts source code into HTML with syntax highlighting. It reads the input file, parses tokens like keywords, comments, strings, and numbers using an event-driven parser, and wraps them in styled HTML tags using CSS. This improves readability and visualization of code.
